apiVersion: v1
kind: Service
metadata:
  name: {{ include "mojito.fullname" . }}
  labels:
    {{- include "mojito.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mojito.selectorLabels" . | nindent 4 }}
